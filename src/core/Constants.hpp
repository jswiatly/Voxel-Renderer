#pragma once

#include <string>

inline const std::string TEXTURE_PATH = "textures/test.jpg";

const int MAX_FRAMES_IN_FLIGHT = 2;

inline constexpr float MAX_RENDER_DISTANCE = 1024.0f;

constexpr int WIDTH = 1600;
constexpr int HEIGHT = 900;
